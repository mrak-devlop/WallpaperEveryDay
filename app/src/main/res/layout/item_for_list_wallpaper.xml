<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/Layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">



    <com.google.android.material.card.MaterialCardView
        android:id="@+id/wallpaperCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:checkable="true"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:strokeWidth= "0dp">

        <ImageView
            android:id="@+id/wallpaperImageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="wallpaper image"
            android:transitionName="@string/wallpaper_transition_name"
            app:srcCompat="@drawable/th" />

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/dateTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/foreground_text"
        android:paddingVertical="2dp"
        android:paddingStart="4dp"
        android:text="Date"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="@id/wallpaperCard"
        app:layout_constraintStart_toStartOf="@id/wallpaperCard"
        app:layout_constraintTop_toTopOf="@id/wallpaperCard" />

</androidx.constraintlayout.widget.ConstraintLayout>